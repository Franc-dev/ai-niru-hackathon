[build]
dockerfilePath = "Dockerfile"
# When Root Directory = backend, Railway uses backend/Dockerfile

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 60
startupTimeout = 120
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
